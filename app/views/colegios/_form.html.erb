<%= form_with(model: colegio) do |form| %>
  <% if colegio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(colegio.errors.count, "error") %> prohibited this colegio from being saved:</h2>

      <ul>
        <% colegio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :direccion, style: "display: block" %>
    <%= form.text_field :direccion %>
  </div>

  <div>
    <%= form.submit "Guardar", class: "btn btn-success mt-3"%>
  </div>
<% end %> 
