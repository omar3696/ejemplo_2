<%= form_with(model: alumno) do |form| %>
  <% if alumno.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(alumno.errors.count, "error") %> prohibited this alumno from being saved:</h2>

      <ul>
        <% alumno.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :colegio_id, style: "display: block" %>
    <%= form.collection_select :colegio_id, @colegios, :id, :nombre, { prompt: "Selecciona un colegio" }, { class: "custom-select" } %>
  </div>

  <div>
    <%= form.submit "Guardar", class: "btn btn-success mt-3"%>
  </div>
<% end %>
