<%= form_with(model: materia) do |form| %>
  <% if materia.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(materia.errors.count, "error") %> prohibited this materia from being saved:</h2>

      <ul>
        <% materia.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :profesor_id, style: "display: block" %>
    <%= form.collection_select :profesor_id, @profesores, :id, :nombre, { prompt: "Selecciona un profesor" }, { class: "custom-select" } %>
  </div>

  <div>
    <%= form.label :colegio_id, style: "display: block" %>
    <%= form.collection_select :colegio_id, @colegios, :id, :nombre, { prompt: "Selecciona un colegio" }, { class: "custom-select" } %>
  </div>


  <div>
    <%= form.submit "Guardar", class: "btn btn-success mt-3"%>
  </div>
<% end %>
